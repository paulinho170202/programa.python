nombre = input("Ingresa tu nombre: ").upper()
sexo = input("Ingresa tu sexo (M para mujer, H para hombre): ").upper()

if (sexo == "M" and nombre < "M") or (sexo == "H" and nombre > "N"):
    grupo = "A"
else:
    grupo = "B"

print("Te corresponde el grupo", grupo)
